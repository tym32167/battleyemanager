export interface IOnlineServer {
    adminsCount:number,
    active: boolean,
    bansCount:number,
    host: string,
    id: number,
    isConnected: boolean,
    name: string,
    playersCount:number,
    port: number,
    steamPort: number,
}