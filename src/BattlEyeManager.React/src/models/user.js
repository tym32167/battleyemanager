export class User
{
    constructor(id, userName, email, lastName, firstName)
    {
        this.id = id;
        this.userName = userName;
        this.email = email;
        this.lastName = lastName;
        this.firstName = firstName;
    }
}